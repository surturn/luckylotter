# Backend (Maven / Java)
backend/target/
*.class

# Frontend (Node / Angular)
frontend/node_modules/
frontend/dist/
frontend/.angular/

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Env / secrets
.env
*.local
.claude
# Playwright MCP artefacts, and the loose screenshots taken while checking a
# change. Screenshots that documentation actually references are not scratch —
# without them docs/presentation.md is a page of broken images.
.playwright-mcp/
*.png
!docs/images/*.png
